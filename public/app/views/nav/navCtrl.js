// INITILIZE CONTROLLER
// ============================================================
angular.module("bash").controller("navCtrl", function($scope) {
  // VARIABLES
  // ============================================================
  $scope.test = "nav test"
  // FUNCTIONS
  // ============================================================
});
